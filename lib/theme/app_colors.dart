import 'dart:ui';

import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color background = Color(0xFF1c1c1c);
  static const Color backgroundGrey = Color(0xFF1c1c1c);
  static const Color accent = Color(0xFFFF9F67);
  static const Color green = Color(0xFF89c589);

  //card colors
  static const Color cardBackground = Color(0xFF282828);  // Lighter than background for contrast
  static const Color cardBackgroundAccent = Color(0xFF323232);
  static const Color cardBorder = Color(0xFF404040);  // Subtle border for depth
  static const Color cardShadowLight = Color(0x40000000);  // Transparent black for shadow
  static const Color cardShadowDark = Color(0x80000000);



  // Text Colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textTertiary = Color(0xFF9E9E9E);

  // UI Element Colors
  static const Color divider = Color(0xFF2C2C2C);
  static const Color bottomNavBackground = Color(0xFF121212);
  static const Color inactive = Color(0xFF4A4A4A);

  // Interaction Colors
  static const Color buttonHover = Color(0xFFFFB088);
  static const Color buttonPressed = Color(0xFFFF8D4D);
}

